$(document).ready(function(){
  var sideAValue = parseInt($("#trianglea").val());
  var sideBValue = parseInt($("#triangleb").val());
  var sideCValue = parseInt($("#trianglec").val());

  if (sideAValue === sideBValue && sideBValue === sideCValue)

});
